name: Variables & Secrets
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  echo-variables:
    environment:
      name: production
    env:
      GREETING_TEXT: "Hello pipelines"
    runs-on: ubuntu-latest
    steps:
      - name: Show secret
        run: echo "${{ secrets.TEST_SECRET }}"
      - name: Show variable
        run: echo "${{ vars.TEST_VARIABLE }}"
      - name: Show alternative syntax
        run: echo $GREETING_TEXT
